VITE_APPS_SCRIPT_URL=
VITE_APP_NAME=FC Abstract Review Assistant
# Local-only App Check debug token. Leave unset to auto-generate one on first run
# (it will be logged to the browser console); put the real value in .env.local, never here.
VITE_APPCHECK_DEBUG_TOKEN=
